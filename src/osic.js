// Operating system info commands

export function osInfo(code) {
    const command = code.split('--')[1];
    switch(command) {
        case 'EOL':
        case 'cpus':
        case 'homedir':
        case 'username':
        case 'architecture':

    }
}